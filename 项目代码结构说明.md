# 项目代码结构说明
分支：`CV-1147-param-analysis`

## 一、项目目标
见文件 recipe项目.txt

## 二、顶层结构
- `README.md`：项目简介
- `requirements.txt`：项目核心依赖（库==版本）
- `scripts/`：运行脚本
- `src/`：核心源码
- `outputs/`：代码输出结果（已 gitignore，不纳入版本控制）
  - `analysis/`：分析报告图表输出
  - `models/`：模型权重与测评
  - `old/`：历史归档（以前的输出结果，仅作留存，不再改动）
- `logs/`：运行日志（已 gitignore）
- `raw_data/`：项目原始数据（已 gitignore，当前用 `raw_data/raw_data_260623/data.parquet`）
- `notebooks/`：用到的 ipynb 文件（暂未纳入追踪）

## 三、当前业务思路
原始数据当前因业务原因在时序上无法得到真实顺序，故分步推进：
1. **当期 tabular 预测**（`src/tasks/current_prediction/`）：用 N 锅次的设定参数与监控参数预测 N 锅次的指标统计量。待业务解决时序问题后再调整数据结构以预测 N+3 锅次。
2. **时序预测**（`src/tasks/forecasting/`，占位）：业务解决时序问题后，引入时序模型预测 N+3 锅次。
3. **参数反向修正**（`src/tasks/param_correction/`，占位）：在物理约束范围内搜索设定参数 S，使前向模型预测指标重新落入合格区间。

## 四、脚本目录
### `scripts/research_scripts/`
研究实验运行脚本，当前包括：
- `run_catboost.sh`：运行 CatBoost 回归实验
- `run_gandalf.sh`：运行 GANDALF 回归实验
- `run_lgbm.sh`：运行 LightGBM 回归实验
- `run_xgb.sh`：运行 XGBoost 回归实验

脚本默认将模型权重与测评输出到 `outputs/models/{model_type}/`，分析报告输出到 `outputs/analysis/`。

## 五、源码目录
### `src/core/` —— 共享基础设施（任务无关）
各任务可复用的能力，模型类型不绑定具体任务。

#### 1. `src/core/constants/`
常量定义：
- `data_constants.py`：数据字段常量（机台、recipe、检测结果列、目标变量分组等）
- `eval_constants.py`：评估参数常量
- `train_constants.py`：训练参数常量

#### 2. `src/core/data/`
数据处理：`preprocess_and_split.py`（业务预处理、特征类型规范、训练/验证切分）

#### 3. `src/core/models/`
模型实现：
- `base.py`：回归模型基类 `BaseRegressor`
- `registry.py`：模型映射 `MODEL_REGISTRY`
- `tabular/`：tabular 模型
  - `xgb_regressor.py` / `lgbm_regressor.py` / `catboost_regressor.py`：单目标树模型
  - `gandalf_regressor.py`：多目标深度模型（pytorch-tabular）
- `timeseries/`：时序模型占位（待步骤2 引入）

#### 4. `src/core/evaluation/`
评估相关：`evaluator.py`（回归评估、SHAP/特征解释、诊断图）

#### 5. `src/core/analysis/`
分析相关：
- `anomaly_mining.py`：预测异常样本挖掘与导出
- `data_description.py`：数据分布描述与 target-feature 散点图

#### 6. `src/core/utils/`
通用工具：`arg_utils.py`（命令行参数构造、日期列解析、输出路径默认值）

### `src/tasks/` —— 按业务阶段划分的任务入口
- `current_prediction/run_regression.py`：步骤1 当期 tabular 回归实验入口，负责根据 `model_type` 选择模型、训练/加载、评估
- `forecasting/`：步骤2 时序预测（占位）
- `param_correction/`：步骤3 参数反向修正（占位）

## 六、输出目录结构
### `outputs/models/` —— 模型权重与测评
```
outputs/models/{model_type}/{model_type}_{date}/
    ├── weights/              # 模型权重 + 元信息（树模型）
    └── eval/                 # metrics / 诊断图 / shap
```
多目标模型（gandalf）按目标分组细分：`{date}/{group_name}/{weights,eval}/`。

### `outputs/analysis/` —— 分析报告图表
- `target_analysis/`：目标变量分布分析
- `anomaly_records/`：预测异常样本记录
- `data_eda/`：原始数据 EDA

### `outputs/old/` —— 历史归档
以前实验的输出结果，按 `analysis/` 与 `models/{weights,eval}/` 归类，仅作留存，不再改动。

## 七、ipynb 文件目录（notebooks/，暂未追踪）
- `analysis_and_model.ipynb`：分析和实验的简单代码
- `raw_data_eda.ipynb`：当前原始数据的 EDA 分析代码

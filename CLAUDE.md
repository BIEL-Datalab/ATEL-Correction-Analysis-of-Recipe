# Claude 协作开发规范

本文档用于约束 Claude 在本项目中的代码修改行为。除非我明确说明，否则 Claude 在分析、修改、重构、调试、补丁修复、测试和提交代码时，都必须严格遵守以下规则。

---

## 1. 总体原则

1. Claude 的首要目标是：**在尽量不破坏现有功能的前提下，完成需求并保证代码可维护性、可回滚性、可测试性和工程规范性**。
2. 所有改动必须遵循“**最小必要修改原则**”：
   - 不随意扩大改动范围；
   - 不顺手修改无关逻辑；
   - 不在没有明确必要时进行大规模重构；
   - 优先局部修复，避免引入额外风险。
3. 在任何实现前，先理解当前代码上下文、调用链、依赖关系和可能影响范围，再开始修改。
4. 当需求不明确、上下文不足、存在多种实现方案且影响较大时，应先给出方案和风险，向我询问细节要求，再执行修改。
5. 修改时要兼顾：
   - 正确性
   - 可读性
   - 可维护性
   - 一致性
   - 工程化规范

---

## 2. Git 与提交要求

1. **每次开始正式修改前，必须先进行一次 commit 备份。**
   - 目的：确保任意阶段都可以快速回滚。
   - 如果改动过程中分成多个关键步骤，每完成一个关键步骤后都应单独 commit。
2. 提交粒度要求：
   - 一个 commit 只做一类清晰的事情；
   - 避免把无关改动混在同一个 commit 中；
   - 提交历史应具备可读性和可追踪性。
3. commit message 要清晰，建议遵循以下格式：
   - `backup: before modifying xxx`
   - `feat: add xxx`
   - `fix: resolve xxx`
   - `refactor: optimize xxx without behavior changes`
   - `docs: update comments or documentation for xxx`
   - `test: add tests for xxx`
4. 在未确认代码稳定前，不要覆盖历史，不要随意 squash，不要执行高风险 Git 操作。
5. 除非我明确要求，否则不要执行以下操作：
   - 强制 push
   - reset --hard
   - rebase 改写公共历史
   - 删除分支
   - 删除重要文件
6. 每次完成修改后，应说明：
   - 改了哪些文件
   - 为什么这样改
   - 风险点是什么
   - 如何回滚

---

## 3. 编码与实现要求

1. 代码必须满足基本工程化要求：
   - 结构清晰
   - 命名准确
   - 模块职责单一
   - 避免重复代码
   - 避免过度耦合
2. 优先复用已有能力：
   - 优先复用现有工具函数、公共组件、基础设施；
   - 不重复造轮子；
   - 新增逻辑前先检查项目内是否已有类似实现。
3. 命名规范：
   - 变量、函数、类、模块名应清晰表达语义；
   - 禁止使用含义模糊的命名，如 `tmp`、`data2`、`handlerNew`，除非是极小局部临时变量；
   - 布尔值变量命名应体现真假语义，如 `isEnabled`、`hasPermission`、`canRetry`。
4. 函数设计要求：
   - 单个函数职责尽量单一；
   - 避免超长函数；
   - 参数数量过多时应考虑对象封装；
   - 尽量减少隐式副作用。
5. 错误处理要求：
   - 对可能失败的逻辑必须进行合理的错误处理；
   - 不允许吞异常；
   - 错误信息应便于定位问题；
   - 必要时增加日志，但不要输出敏感信息。
6. 对性能敏感路径：
   - 避免不必要的循环、重复请求、重复计算；
   - 避免引入明显的性能退化；
   - 大改前需说明复杂度和性能影响。
7. 对兼容性敏感逻辑：
   - 修改前分析上下游影响；
   - 尽量保持原接口兼容；
   - 若无法兼容，必须明确标记破坏性变更。

---

## 4. 注释与文档要求

1. **代码需要有必要的注释，但注释必须有价值，不能为了注释而注释。**
2. 以下场景必须补充注释：
   - 业务规则复杂；
   - 逻辑不直观；
   - 存在特殊边界条件；
   - 某种写法是为了兼容旧逻辑、性能优化或规避历史问题；
   - 第三方库行为容易误解。
3. 注释应解释：
   - “为什么这样做”
   - “这样做要注意什么”
   而不仅仅是重复代码表面含义。
4. 对新增模块、核心函数、复杂配置，必要时同步更新相关文档。
5. 如果发现旧代码缺乏关键注释，在不扩大无关改动的前提下，可补充必要注释。

---

## 5. 测试与验证要求

1. 每次代码修改后，必须尽可能进行验证。
2. 验证方式包括但不限于：
   - 运行已有测试；
   - 补充必要单元测试；
   - 补充集成测试或接口测试；
   - 对关键路径进行最小可行验证。
3. 如果当前环境无法执行测试，必须明确说明：
   - 哪些测试本应执行；
   - 为什么没执行；
   - 存在什么风险。
4. 修复 bug 时，优先补充能覆盖该 bug 的测试用例，避免回归。
5. 不允许只改代码不说明验证结果。

---

## 6. 输出与沟通要求

Claude 每次响应应尽量遵循以下结构：

### 6.1 修改前
先说明：
1. 对需求的理解；
2. 计划修改哪些文件；
3. 是否存在风险或待确认点；
4. 将如何分步骤执行；
5. 是否需要先做 commit 备份。

### 6.2 修改中
如果分多步执行，每完成一步应说明：
1. 当前完成了什么；
2. 修改了哪些文件；
3. 是否已进行 commit 备份；
4. 下一步要做什么。

### 6.3 修改后
必须输出：
1. 本次修改摘要；
2. 涉及文件列表；
3. 核心实现说明；
4. 测试/验证结果；
5. 风险与注意事项；
6. 回滚方式；
7. 建议的 commit message。

---

## 7. 安全与风险控制要求

1. 未经明确要求，不得引入以下高风险行为：
   - 删除核心逻辑
   - 大规模重构
   - 批量替换底层依赖
   - 修改数据库结构
   - 修改线上配置
   - 修改鉴权、支付、权限、风控等高敏感逻辑
2. 涉及以下内容时必须格外谨慎并先说明风险：
   - 数据库迁移
   - 缓存一致性
   - 并发问题
   - 事务边界
   - 权限控制
   - 密钥、token、环境变量
   - 对外接口兼容性
3. 不得输出或硬编码敏感信息，包括但不限于：
   - 密码
   - token
   - 密钥
   - 私有证书
   - 生产环境连接信息

---

## 8. 重构要求

1. 没有明确要求时，不要为了“看起来更优雅”而大改现有代码。
2. 若确实需要重构，应满足：
   - 有明确收益；
   - 改动范围可控；
   - 不改变外部行为；
   - 有验证手段；
   - 最好分步提交，确保可回滚。
3. 重构时要保留业务语义，不得因风格统一而破坏原有逻辑。

---

## 9. 依赖与配置变更要求

1. 新增依赖前必须先评估：
   - 是否已有现成依赖可用；
   - 是否可以用项目现有能力实现；
   - 依赖体积、维护成本、安全风险如何。
2. 未经必要说明，不要随意升级核心依赖版本。
3. 修改配置文件时必须说明影响范围，包括：
   - 本地开发
   - 测试环境
   - 构建流程
   - 部署流程

---

## 10. 面向高质量代码的补充要求

1. 优先写出**可读、稳定、可维护**的代码，而不是“技巧性强但难理解”的代码。
2. 避免魔法值，必要时提取常量。
3. 避免隐式约定，必要时通过类型、注释、校验明确约束。
4. 对边界情况进行考虑，例如：
   - 空值
   - 超时
   - 重试
   - 重复提交
   - 并发覆盖
   - 数据格式异常
5. 若项目支持类型系统（如 TypeScript），尽量补全类型，避免滥用 `any`。
6. 若项目有 lint、format、test、build 规范，修改后应尽量确保通过。
7. 修改代码时应主动检查是否会影响：
   - 现有测试
   - 编译构建
   - 导入导出关系
   - 调用方兼容性
   - 日志与监控

---

## 11. 禁止事项

除非我明确要求，否则 Claude 不应：

1. 擅自修改和移动与当前任务无关的文件；
2. 擅自进行大规模代码风格清理；
3. 擅自重命名大量文件/符号；
4. 擅自删除注释、测试或文档；
5. 擅自引入新依赖；
6. 擅自修改 CI/CD、部署、数据库结构；
7. 擅自执行不可逆操作；
8. 在没有验证的情况下声称“已经修复”。

---

## 12. 标准执行流程

对于每一次任务，Claude 应默认遵循以下流程：

1. 阅读需求并复述理解；
2. 分析相关代码、依赖和影响范围；
3. 先执行一次 commit 备份；
4. 给出修改计划；
5. 按最小改动原则实施修改；
6. 为关键逻辑补充必要注释；
7. 执行测试/验证；
8. 输出修改总结、风险和回滚方案；
9. 在关键阶段进行额外 commit，保证过程可追踪、可回滚。

---

## 13. 每次开始工作的固定遵循语句

当我要求你修改代码时，你必须默认遵循本文档全部要求，尤其是：

- 先理解再修改；
- 先备份 commit 再改动；
- 保持最小必要修改；
- 补充必要注释；
- 满足工程化规范；
- 做必要验证；
- 输出修改说明、风险说明和回滚方式。

如果某项要求由于环境限制无法执行，你必须明确说明原因，并给出替代方案。

---